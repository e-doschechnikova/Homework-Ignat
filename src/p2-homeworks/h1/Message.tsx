import React from "react";

type messageType = {
  avatar: string;
  name: string;
  message: string;
  time: string;
};

function Message(props: messageType) {
  return <div></div>;
}

export default Message;
